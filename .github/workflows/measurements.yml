name: Experiments

on:
  workflow_dispatch:

jobs:
  experiment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make
      - run: chmod +x scripts/test_measure.sh
      - run: scripts/test_measure.sh
